const GAME_START = 0;
const GAME_PLAY = 1;
const GAME_OVER = 2;
const GAME_WIN = 3;
const GAME_PAUSE = 4;

const rotationSpeed = 0.02;
const moveSpeedForward = 0.001;
const moveSpeedBackward = 0.01;
const maxSpeedForward = 0.1;
const maxSpeedBackward = -0.01;
const mileage = 60;

export {
  GAME_START,
  GAME_PLAY,
  GAME_OVER,
  GAME_WIN,
  GAME_PAUSE,
  rotationSpeed,
  moveSpeedForward,
  moveSpeedBackward,
  maxSpeedForward,
  maxSpeedBackward,
  mileage
};
